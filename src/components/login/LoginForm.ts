import styled from "styled-components";

const Login = styled.form``;

const InputId = styled.input``;

const InputPw = styled.input``;

const LoginRequest = styled.button``;

export { Login, InputId, InputPw, LoginRequest };
